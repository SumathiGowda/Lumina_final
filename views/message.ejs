<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Submitted</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: url('6.jpg') no-repeat center center fixed;
            background-size: cover;
        }

        .message-container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 600px;
            margin: 20px;
        }

        h1 {
            color: #2ecc71;
            margin-bottom: 1rem;
            font-size: 2rem;
        }

        p {
            color: #333;
            font-size: 1.1rem;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .btn {
            padding: 12px 30px;
            font-size: 1.1rem;
            background: linear-gradient(45deg, #6a666e, #999251);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: transform 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="message-container">
        <h1>🎉 Your post has been successfully submitted and saved!</h1>
        <p>Your post is under review. If accepted, it will be posted directly. Thank you!</p>
        <a href="/pfp" class="btn">Back to Profile</a>
    </div>
</body>
</html>